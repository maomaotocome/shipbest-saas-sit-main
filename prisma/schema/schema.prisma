datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  relationMode      = "prisma" // without foreign keys
}

generator client {
  provider = "prisma-client-js"
  output   = "../../src/db/generated/prisma"
}
